<Window x:Class="BudgetTracker.Wpf.Net8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:BudgetTracker.Wpf.Net8.ViewModels"
        xmlns:conv="clr-namespace:BudgetTracker.Wpf.Net8.Converters"
        xmlns:views="clr-namespace:BudgetTracker.Wpf.Net8.Views"
        Title="Budget Tracker"
        Width="1000"
        Height="650">

    <Window.Resources>
        <conv:DecimalSignToBrushConverter x:Key="NetBrush" />
    </Window.Resources>

    <Grid>
        <TabControl>

            <!-- ================= Transactions ================= -->
            <TabItem Header="Transactions">
                <views:TransactionsView />
            </TabItem>

            <TabItem Header="Categories" />
            <TabItem Header="Budgets" />

            <!-- ================= Summary ================= -->
            <TabItem Header="Summary">
                <TabItem.DataContext>
                    <vm:SummaryViewModel />
                </TabItem.DataContext>

                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Month / Year -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Month:" Margin="0,0,6,0"/>
                        <ComboBox Width="80"
                                  ItemsSource="{Binding Months}"
                                  SelectedItem="{Binding SelectedMonth}" />

                        <TextBlock Text="Year:" Margin="16,0,6,0"/>
                        <ComboBox Width="100"
                                  ItemsSource="{Binding Years}"
                                  SelectedItem="{Binding SelectedYear}" />
                    </StackPanel>

                    <!-- Totals -->
                    <Border Grid.Row="1" Padding="12" CornerRadius="8"
                            BorderThickness="1" BorderBrush="#33000000"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="Total Income" Opacity="0.7"/>
                                <TextBlock Text="{Binding TotalIncome, StringFormat=C}"
                                           FontSize="18" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Total Expense" Opacity="0.7"/>
                                <TextBlock Text="{Binding TotalExpense, StringFormat=C}"
                                           FontSize="18" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Net Total" Opacity="0.7"/>
                                <TextBlock Text="{Binding NetTotal, StringFormat=C}"
                                           FontSize="18" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Category Breakdown -->
                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding CategorySummaries}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category"
                                                Binding="{Binding CategoryName}"
                                                Width="2*" />

                            <DataGridTextColumn Header="Income"
                                                Binding="{Binding TotalIncome, StringFormat=C}" />

                            <DataGridTextColumn Header="Expense"
                                                Binding="{Binding TotalExpense, StringFormat=C}" />

                            <DataGridTemplateColumn Header="Net">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NetAmount, StringFormat=C}"
                                                   FontWeight="SemiBold"
                                                   Foreground="{Binding NetAmount, Converter={StaticResource NetBrush}}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
